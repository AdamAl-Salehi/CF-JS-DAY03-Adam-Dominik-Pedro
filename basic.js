function formas(width, height, depth){
	var area = width * height ;
	var vol = width * height * depth ; 
	document.write("The area of the box is:"+area+"<br> The volume of the box is:" +vol);
}
formas(10, 10, 20);